#include "./thread.h"
#include "../thread.h"

#include "../../testing_src/assert.h"
#include <inttypes.h>

const chunit_test_suite UTIL_TEST_SUITE_THREAD = {
    .name = "Threading Suite",
    .tests = {

    },
    .tests_len = 0
};
